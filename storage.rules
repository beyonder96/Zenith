rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Regra para fotos de pets
    match /pets/{userId}/{fileName} {
      allow read: if true;
      allow write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
