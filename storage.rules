rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    
    match /pets/{userId}/{folder}/{fileName} {
      allow read: if true;
      allow write: if request.auth != null && request.auth.uid == userId 
                   && (folder == 'photos' || folder == 'rga');
    }
  }
}
